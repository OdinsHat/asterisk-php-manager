<!-- $Revision: 1.1 $ -->
<refentry id="package.http.http-request.proxy-auth">
 <refnamediv>
  <refname>Proxy Authorization</refname>
  <refpurpose>
   Making use of a HTTP proxy
  </refpurpose>
 </refnamediv>

 <refsect1 id="package.http.http-request.proxy-auth.usage">
  <title>How to use Proxy Authorization</title>

  <example>
   <title>Using a proxy server with anonymous access</title>

   <para>
    In this example it is assumed that one wants to use the machine with
    the hostname <literal>proxy.example.com</literal>, where a proxy
    server is listening on port <literal>8080</literal>, to proxy the
    outgoing connection to example.com.
   </para>

   <para>
    The second parameter of <function>setProxy</function> is optional
    and defaults to <literal>8080</literal>.
   </para>

   <programlisting role="php">
require_once "HTTP/Request.php";

$req =&amp; new HTTP_Request("http://example.com/");
$req->setProxy("proxy.example.com", 8080);
   </programlisting>
  </example>

  <example>
   <title>Using proxy authorization</title>

   <para>
    This is the same example as above, except that a username/password
    tuple is provided, which authorizes the user at the proxy server:
    The username is <literal>johndoe</literal> and the appendant
    password is <literal>foo</literal>.
   </para>

   <programlisting role="php">
require_once "HTTP/Request.php";

$req =&amp; new HTTP_Request("http://example.com/");
$req->setProxy("proxy.example.com", 8080, "johndoe", "foo");
   </programlisting>

  </example>
 </refsect1>

</refentry>
<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->  
